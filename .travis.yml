language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t albaalah/react-docker-app -f Dockerfile.dev .

script:
  - docker run -e CI=true albaalah/react-docker-app npm run test